# Relay execution-plan convention

The active implementation plan is `.agent/relay-p0-execplan.md`.

Keep it current after every green checkpoint. Each checkpoint must record the
completed outcome, validation evidence, changed files, unresolved risk, and the
next concrete goal. A future session should be able to resume from it without
conversation history.

